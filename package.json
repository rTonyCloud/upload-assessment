{
    "name": "upload-assessment",
    "version": "1.0.0",
    "description": "An upload assessment project",
    "main": "server/server.ts",
    "scripts": {
        "start": "concurrently \"node server/server.ts\" \"cd ../client && npm run start\"",
        "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
        "install": "cd server && npm i && cd ../client && npm i",
        "seed": "cd server && npm run seed",
        "build": "cd client && npm run build",
        "lint": "concurrently \"cd server && npm run lint\" \"cd ../client && npm run lint\"",
        "format": "concurrently \"cd server && npm run format\" \"cd .. client && npm run format\""
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/node": "^20.11.5",
        "concurrently": "^8.2.2",
        "prettier": "^3.1.1"
    }
}
